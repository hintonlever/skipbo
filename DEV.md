# Dev Setup

## Build WASM (required before first run, and after any C++ changes)

```bash
bash scripts/build-wasm.sh
```

## Run Web UI

```bash
cd web
npm install   # first time only
npm run dev
```

Opens at http://localhost:5173
